Ты работаешь ЛОКАЛЬНО в проекте:
E:\GPT1C_Processor_analitica

ЦЕЛЬ:
Полностью автономно проверить логи, найти реальные баги, доказать причины, внести минимальные исправления и проверить результат.

ОСНОВНОЕ ПРАВИЛО:
Пользователь НЕ должен вручную смотреть логи и искать причины.
Это должны сделать ты и другие агенты/контуры автоматически.
Ты берёшь на себя полный цикл:
анализ логов -> поиск причины -> минимальный патч -> проверки -> локальный коммит.

КОНТЕКСТ:
1. Проект локальный. Никакого подключения оркестратора к репозиторию не делать.
2. AGENTS.md уже существует и является мастер-документом проекта. Его не заменять.
3. Оркестратор уже создан и закоммичен локально.
4. Есть worker-once / complete / review-once / cooldown-логика.
5. Планировщик Windows будет работать через worker-once.
6. Claude может быть на лимите. Это не должно блокировать анализ и исправления.
7. Если Claude недоступен, работа продолжается через доступный контур, а review-этап помечается корректно, без поломки active_task.
8. Нужно смотреть именно реальные логи и состояния, а не гадать.
9. Исправлять только подтверждённые баги.

НЕ ТРОГАТЬ:
- config/collector_prompts.json
- audit/*
- ORCHESTRATOR_CONTEXT_2026-04-23.md
- ORCHESTRATOR_CONTEXT_2026-04-23_updated.md
- ORCHESTRATOR_CONTEXT_2026-04-23_v2.md
- любые несвязанные файлы проекта
- не менять бизнес-логику основного бота, кроме того, что прямо связано с оркестратором и автоматическим циклом агентов

РАЗРЕШЕНО ТРОГАТЬ:
- orchestrator.py
- orchestrator_agents.json
- orchestrator_queue.json
- orchestrator_state.json
- orchestrator_task_template.json
- task_*.json, если это нужно для корректного теста автоматики
- .ai_reviews/*
- только связанные с оркестратором и автоматизацией файлы

ЧТО ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ПРАВКАМИ:
1. AGENTS.md
2. SESSION_CONTEXT.md, если файл существует
3. repo_map.json, если файл существует
4. ORCHESTRATOR_CONTEXT_2026-04-23_v2.md
5. orchestrator.py
6. orchestrator_agents.json
7. orchestrator_queue.json
8. orchestrator_state.json
9. orchestrator_task_template.json
10. task_001.json, если он существует
11. актуальные файлы логов в .ai_logs/
12. любые логи оркестратора и агентных запусков, которые относятся к текущему состоянию

ТВОЙ ПОРЯДОК РАБОТЫ:
1. Выполни git status --short --branch
2. Явно отдели связанные файлы от несвязанных
3. Сам прочитай контекст и логи
4. Найди только реальные баги, подтверждённые логами/состоянием/кодом
5. Для каждого бага:
   - покажи симптом
   - покажи доказательство из логов/JSON/кода
   - сформулируй точную причину
   - внеси минимальный патч
6. После правок обязательно:
   - bump версии изменённых runtime-файлов на +0.0.1
   - python -m py_compile для изменённых runtime-файлов
   - выполнить узкие smoke-проверки именно по затронутому сценарию
7. Если баг связан с очередью/active_task/history/review/rate_limit/cooldown:
   - проверь переходы состояний
   - проверь, что active_task не теряется
   - проверь, что очередь не ломается
8. Если видишь лимит агента:
   - не помечай задачу как ложный failed
   - обрабатывай это как отдельный технический статус
9. Логи и артефакты не игнорировать.
10. Не останавливаться на общем анализе — реально исправить найденные баги.

ПРИОРИТЕТЫ ПОИСКА БАГОВ:
1. worker-once / review-once / complete
2. ложные rate_limited
3. повреждение active_task / tasks / history
4. баги переходов queued -> running -> review -> done
5. bugs в cooldown Codex/Claude
6. баги сохранения stdout/stderr/result
7. кодировка / mojibake, если она реально ломает данные или автоматизацию
8. ошибки, мешающие Планировщику Windows

ВАЖНО ПО ЛОГАМ:
- не анализировать всё подряд без разбора;
- анализировать только актуальные логи и состояния, относящиеся к текущему запуску/текущему task_id;
- отделять шум от причины;
- предупреждения не считать багом, если они не ломают исполнение.

ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ:
- python -m py_compile orchestrator.py
- если меняешь другие runtime-файлы — py_compile и их тоже
- точечные smoke-тесты по изменённой логике:
  - status
  - enqueue
  - worker-once
  - review-once
  - complete
  - поведение при пустой очереди
  - поведение при active_task
  - поведение при cooldown/rate-limited
- не запускать лишние широкие тесты без необходимости

УСЛОВИЯ ЗАВЕРШЕНИЯ:
Задача считается выполненной только если:
1. найден хотя бы один реальный баг ИЛИ честно доказано, что новых багов в текущем контуре не найдено;
2. все найденные баги либо исправлены, либо точно локализованы с доказательствами;
3. проверки выполнены;
4. сделан локальный commit только по связанным файлам;
5. несвязанные файлы не тронуты.

ФОРМАТ ФИНАЛЬНОГО ОТВЕТА:
1. Что проверено
2. Какие баги реально найдены
3. Какие файлы изменены
4. Какие правки внесены
5. Какие проверки выполнены и результат
6. Локальный commit SHA
7. Что осталось нерешённым
8. Следующий рекомендуемый шаг

ВАЖНО:
- не проси пользователя вручную смотреть логи;
- не перекладывай расследование на пользователя;
- не делай вид, что баг есть, если доказательств нет;
- не трогай несвязанные файлы;
- работай до законченного инженерного результата.