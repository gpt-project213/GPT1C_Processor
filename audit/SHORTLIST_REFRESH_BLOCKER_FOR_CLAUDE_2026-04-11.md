# SHORTLIST REFRESH BLOCKER FOR CLAUDE

Дата: 2026-04-11

Статус: свежие отчеты скачаны и collector dry-run выполнен, но новый shortlist нельзя считать корректным и нельзя отдавать менеджерам как рабочий список.

Codex не менял бизнес-логику, не запускал WhatsApp send, не включал live, не менял `.env`.

## 1. Что запускалось

### 1.1. Обновление отчетов

Команда:

```powershell
$env:WHATSAPP_ENABLED='0'
$env:LIVE_SEND_ALLOWED='0'
.\.venv\Scripts\python.exe run_pipeline_all_mp.py
```

Факты:

- Старт запуска: 2026-04-11 19:12:02.
- `run_pipeline_all_mp.py` вызвал `imap_fetcher.py --once`.
- IMAP login успешен.
- Найдено 13 писем.
- Отчеты скачаны от `saida111278@mail.ru`.
- После обработки `reports/queue` пустая.
- За запуск в `reports/excel/processed` попало 39 файлов.
- За запуск в `reports/html` создано/обновлено 41 файл.
- За запуск в `reports/json` создано/обновлено 23 JSON-файла.

Safety:

- Общий `bot/send_reports.py` не запускался.
- Telegram polling/scheduler не поднимался.
- `.env` не менялся.
- `WHATSAPP_ENABLED=0` и `LIVE_SEND_ALLOWED=0` были выставлены только в окружении процесса.

### 1.2. Collector dry-run

Команда:

```powershell
$env:WHATSAPP_ENABLED='0'
$env:LIVE_SEND_ALLOWED='0'
.\.venv\Scripts\python.exe -m collector.collections_engine --dry-run
```

Факты из dry-run:

- Загружено debt JSON: 12 файлов.
- Объединено клиентов: 426.
- Классифицировано должников всего: 84.
- Уровни 1-5: 29.
- Обработано коллектором: 1.
- Пропущено stop-list: 83.
- Отправлено сообщений: 0.
- Нет контактов телефона/TG: 1.

Safety:

- Это был `--dry-run`.
- `--send` не запускался.
- `--preview` не запускался.
- Менеджерам Telegram preview не уходил.
- WhatsApp клиентам ничего не отправлялось.

## 2. Почему fresh shortlist заблокирован

Пользователь указал, что список неверный.

Ключевой пример: `А Арман ИП Кыпшак ул Отырар 15/8`.

Пользователь дал расшифровку по Арману из Excel/взаиморасчетов. По этой расшифровке итог на 11.04.2026 выглядит как `0,25`, то есть фактически ноль, а не долг для уведомления.

При этом collector dry-run классифицировал Армана как кандидата:

- клиент: `А Арман ИП Кыпшак ул Отырар 15/8`;
- сумма: `1 093 881,25`;
- просрочка: `12`;
- уровень: `1`;
- активность: `debit=1 934 227,25`, `credit=962 846,00`.

Вывод: Арман не должен попадать в список уведомлений до выяснения причины расхождения.

## 3. Что было видно в свежих JSON по Арману

В свежих JSON от 11.04.2026 найдены записи Армана с такими полями:

- `opening=122500.0`
- `debit=1934227.25`
- `credit=2056727.0`
- `days_silence=3` или `4`

Такие записи найдены в:

- `reports/json/debt_ext_Детальный Дебиторы Алена (134).json`
- `reports/json/debt_ext_Ведомость_по_взаиморасчетам_с_контрагентами_Алена (324).json`
- `reports/json/debt_ext_Детальный_по_взаиморасчетам_с_контрагентами (123).json`

Но объединенная запись, которую увидел collector, стала другой:

- `debt=1093881.25`
- `opening=122500.0`
- `debit=1934227.25`
- `credit=962846.0`
- `days_silence=12`
- `_manager=Алена`

Это значит, что проблема не в том, что Арман реально кандидат. Проблема в данных, объединении или парсинге, которые подаются в collector.

## 4. Вероятная зона проверки для Claude

Это не утверждение о баге, а место, которое надо проверить:

1. `collector/debt_monitor.py:77` - `load_latest_debt_json()`.
2. `collector/debt_monitor.py:137-142` - при дублях выбирается запись с большим `days_silence`.
3. Возможный риск: старая или менее корректная запись может перебить свежую запись, если у нее больше дней молчания.
4. `debt_auto_report.py:741` вызывает `ensure_clean_xlsx(src, force_fix=True)`, то есть clean-копия должна создаваться.
5. Нужно проверить, почему после clean/parse свежий JSON и merged result дают разные финансовые остатки.

## 5. Что Claude должен проверить по Арману

Перед любыми правками collector:

1. Открыть исходный Excel по свежему отчету Алены.
2. Открыть clean-копию этого Excel.
3. Открыть свежий JSON по Алене.
4. Открыть результат `load_latest_debt_json()`.
5. Сравнить:
   - итоговый долг/closing;
   - `opening`;
   - `debit`;
   - `credit`;
   - `days_silence`;
   - источник записи, которая попала в merged clients.

Ожидаемый бизнес-факт от пользователя: Арман фактически ноль, остаток `0,25`.

## 6. Текущий безопасный вывод

До исправления данных/merge:

- Армана нельзя уведомлять.
- Новый список 29 клиентов нельзя отправлять менеджерам как правильный.
- Dry-run полезен только как evidence of bug: он показывает расхождение между Excel-бизнес-остатком и collector-классификацией.
- Manager shortlist надо пересобрать только после проверки clean Excel и merge-логики.

## 7. Что важно по Саиде

Пользователь отдельно уточнил: Саида часто знает об оплате раньше, чем она разнесена в 1С. Разноска может задерживаться на один день.

Значит, для будущей логики Claude:

- 1С не всегда единственный оперативный источник истины в течение одного дня.
- Саида должна быть отдельным источником факта оплаты.
- Если Саида подтверждает оплату, клиент не должен получать жесткое WhatsApp-напоминание только потому, что 1С еще не обновилась.
- Нужен промежуточный статус вроде `payment_received_unposted` или аккуратное использование `conditional`.

Связанный документ:

- `audit/SAIDA_PAYMENT_FLOW_AUDIT_2026-04-11.md`

## 8. Encoding / mojibake note

Пользователь отдельно указал: крякозябры вида `РљР»...` не должны быть в проекте, если это не осознанно необходимо.

Во время чтения через PowerShell часть русских строк отображалась как mojibake, например:

```text
РљР»Р°СЃСЃРёС„РёС†РёСЂРѕРІР°РЅРѕ %d РґРѕР»Р¶РЅРёРєРѕРІ
```

Что важно:

- Не надо массово чинить кодировку вслепую.
- Надо различать реальную порчу файла и неправильный вывод консоли.
- Новые русские тексты Claude должен сохранять нормальным UTF-8.
- Если найден реальный mojibake в рабочем коде, чинить отдельной маленькой правкой, не смешивать с бизнес-логикой collector.

## 9. Что Claude НЕ должен делать сразу

- Не использовать fresh shortlist на 29 клиентов как рабочий.
- Не отправлять этот список менеджерам.
- Не включать WhatsApp live.
- Не делать `--send`.
- Не считать `active_blocked` автоматическим кандидатом.
- Не считать `stop_registry` автоматическим запретом или автоматическим разрешением на уведомление.
- Не править merge/parse без воспроизведения кейса Армана.

## 10. Следующий правильный шаг

Один узкий шаг:

Воспроизвести кейс Армана от исходного Excel до merged collector record и понять, где сумма `0,25` превращается в `1 093 881,25`.

Только после этого пересобирать список кандидатов для менеджеров.

