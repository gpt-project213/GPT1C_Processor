# Полный свод аудита отчётов (Excel → JSON/HTML → бот)

**Дата:** 2026-04-14
**Обновление свода:** 2026-04-15 — фактическая проверка статусов по коду + тестам (8/8 ✅)
**Корень проекта:** `E:\GPT1C_Processor_analitica`
**Метод:** параллельный обзор исходников (5 суб-агентов) + верификация ведущим агентом по коду.

## Ограничения

- Каталоги `reports/html/` и `reports/analytics/` в среде разработки могут быть в `.cursorignore` — готовые HTML на диске не всегда читаются инструментами; достоверность проверялась по **генераторам** (Python + `templates/`).
- `config/*.json` в репозитории могут отсутствовать (gitignore) — поведение fallback описано в коде.

---

## CRITICAL

| ID | Модуль | Проблема | Статус (2026-04-15) |
|----|--------|----------|----------------------|
| **C1** | `inventory_turnover_report.py` | В докстринге/HTML заявлено «>30 дней без продаж», фактически критерий: **нет строки товара в объединённом `sales_*.json` за выбранный период** + `total_cost > 1000`. При **пустом `sales_dict`** почти весь остаток попадает в «мёртвый запас». | ⚠️ **ЧАСТИЧНО** — early exit при пустом словаре добавлен (v1.1.5). Алгоритм "N дней без продаж" **не реализован** — по-прежнему критерий "нет в JSON". Открыто. |

---

## HIGH

| ID | Модуль | Проблема | Статус (2026-04-15) |
|----|--------|----------|----------------------|
| **H1** | `rfm_clients_report.py` | VIP/лояльные от глобального `total_revenue`, а отчёт по менеджеру | ✅ **ИСПРАВЛЕНО** v1.1.7 — знаменатель = `managers_data[manager]["total_revenue"]` (выручка менеджера). Подтверждено grep + тест `test_vip_uses_manager_denominator`. |
| **H2** | `gross_report_pct.py` | Парсинг `%`: запятая удалялась до `replace(",",".")` → `12,5` → `125` | ✅ **ИСПРАВЛЕНО** v1.5.8 — `_money_to_float` через `[^\d.,-]` с сохранением запятой. Тест `test_comma_decimal_percent` ✅. |
| **H3** | `gross_report_pct.py` | `_find_hdr_row` отличался от `gross_report.py` | ✅ **ИСПРАВЛЕНО** v1.5.8 — импортирован `_find_hdr_row` из `gross_report`. |
| **H4** | `sales_report.py` / `sales_parser.py` | Разный порог конца таблицы: 2 vs 3 пустых строки | ✅ **ИСПРАВЛЕНО** — оба используют `empty >= 2`. Тест `test_two_blanks_end` ✅. Полная унификация классификации строк на экзотических выгрузках — при необходимости отдельная проверка на реальных файлах. |
| **H5** | `revenue_concentration_report.py` | Лишний `</div>` после `</table>` в manager-версии HTML | ✅ **ИСПРАВЛЕНО** v1.1.6 — структура исправлена, тест `test_pct_version_wraps_table_before_footer` ✅. |
| **H6** | `debt_auto_report.py` | `find_header`: тихий fallback `[0, 1]` вместо ошибки | ✅ **ИСПРАВЛЕНО** v2.7.7 — `raise ValueError(...)`. Тест `test_raises_without_real_header` ✅. |

---

## MEDIUM (достоверность / UX)

| ID | Модуль | Проблема | Статус (2026-04-15) |
|----|--------|----------|----------------------|
| **M1** | `revenue_concentration_report.py` | `merged["total_revenue"]` не пересчитывался после дедупа | ✅ **ИСПРАВЛЕНО** v1.1.6 — `merged["total_revenue"] = merged_revenue` (сумма по clients). Тест `test_sum_matches_clients` ✅. |
| **M2** | `revenue_concentration_report.py` | KPI = доля топ-5, подписан как «Парето 80/20» | ✅ **ИСПРАВЛЕНО** v1.1.7 — добавлен дисклеймер «доля выручки пяти крупнейших клиентов; не эквивалентен полному правилу Парето 80/20». Тест `test_admin_html_contains_clarification` ✅. |
| **M3** | `sales_profitability_report.py` | Средняя маржа по клиенту только по сматченным позициям | ✅ **ИСПРАВЛЕНО** v1.0.5 — в JSON поле `revenue_with_margin`; в HTML пояснение "по выручке с известной маржой: X из Y общей". |
| **M4** | `inventory_turnover_report.py` | Нормализация названия товара слабая — ложные «нет продаж» | ✅ **ИСПРАВЛЕНО** v1.1.6 — `normalize_product`: `кг.` → `кг`, `kg.` → `kg`. Тест `test_kg_dot_matches_kg` ✅. |
| **M5** | `inventory.py` | Строки без классификации молча пропускались | ✅ **ИСПРАВЛЕНО** v1.1.7 — `LOG.debug` + счётчик + `LOG.info` итого. |
| **M6** | `expenses_parser.py` | Inline HTML без `@media`, без `.table-wrap`, палитра не из брендбука | ✅ **ИСПРАВЛЕНО** v1.1.2 — `.table-wrap { overflow-x:auto }`, `@media (max-width:768px)`, brand palette. |
| **M7** | `inventory_cost_parser.py` | Inline HTML: слабая мобильная вёрстка | ✅ **ИСПРАВЛЕНО** v1.6.8 — `.table-wrap`, `@media`, brand palette. |
| **M8** | `gross_report_pct.py` | Имя товара без `html.escape` | ✅ **ИСПРАВЛЕНО** v1.5.8 — `html_stdlib.escape(str(r['product']), quote=True)`. |

---

## LOW

| Пункт | Статус (2026-04-15) |
|-------|----------------------|
| `templates/debt_auto.html`: `font-size:11px` на 480px, сброс `min-width` | ✅ **ИСПРАВЛЕНО** — `12px` по всему шаблону, таблица в `.table-container { overflow:auto }`. |
| `sales_report.py`: опечатка `школa` (лат. `a`), `exc_info=e` вместо `exc_info=True` | ✅ **ИСПРАВЛЕНО** v1.0.7. |
| `debt_auto_report.py`: неиспользуемый `NBSP` | ✅ **ИСПРАВЛЕНО** v2.7.8 — константа удалена. |
| `rfm_clients_report.py`: footer 11px | ✅ **ИСПРАВЛЕНО** v1.1.8 — footer 12px. |
| `net_profit_report.py`: docstring расхождение с логикой | ✅ **ИСПРАВЛЕНО** v1.2.7 — docstring приведён к факту, footer/карточки ≥12px. |
| Дублирование CSS между шаблонами vs `base.html` | ⚠️ **ОТКРЫТО** — архитектурное, ARCH-1/3 из CLAUDE.md. Не входило в этот аудит. |

---

## Тесты (2026-04-15)

```
python -X utf8 tests/test_audit_reports_20260414.py
Ran 8 tests in 0.397s — OK
```

| Тест | Покрывает | Результат |
|------|-----------|-----------|
| `test_pct_version_wraps_table_before_footer` | H5 | ✅ |
| `test_sum_matches_clients` | M1 | ✅ |
| `test_admin_html_contains_clarification` | M2 | ✅ |
| `test_raises_without_real_header` | H6 | ✅ |
| `test_comma_decimal_percent` | H2 | ✅ |
| `test_vip_uses_manager_denominator` | H1 | ✅ |
| `test_two_blanks_end` | H4 | ✅ |
| `test_kg_dot_matches_kg` | M4 | ✅ |

---

## Остаётся открытым

| Тема | Описание | Приоритет |
|------|----------|-----------|
| **C1 (алгоритм)** | «Мёртвый запас = N календарных дней без продаж» **не реализовано**. Критерий — отсутствие товара в `sales_*.json` за период. При желании — отдельная реализация по дате последней продажи из JSON. | MEDIUM |
| **DSO UX** | HTML: «DSO = дебиторка ÷ средн. дн. выручка» (строка 449) — корректная подпись добавлена (v1.3.2). Открытый вопрос: крупный заголовок карточки — нужно ли переименовать «Средний срок оплаты» → «DSO» или оставить текущий вид. Функционально не баг. | LOW |
| **H4 (хвост)** | Выровнено 2 пустых строки; полная унификация **классификации строк** между `sales_report` и `sales_parser` на экзотических выгрузках — при необходимости отдельная проверка на реальных файлах. | LOW |
| **ARCH** | Дублирование CSS / `base.html` (`ARCH-1` из CLAUDE.md); inline HTML в `expenses_parser` / `inventory_cost_parser` остаётся архитектурно, частично смягчено по UX (волна 2). | ARCH |

---

## Итог

| Тип | Всего | Исправлено | Открыто |
|-----|-------|-----------|---------|
| CRITICAL | 1 | 0 (partial) | 1 |
| HIGH | 6 | 6 | 0 |
| MEDIUM | 8 | 8 | 0 |
| LOW | 5 | 5 | 0 |
| ARCH | 1 | 0 | 1 |

**Все HIGH и MEDIUM закрыты. Все 8 тестов зелёные.**

*Конец свода.*
